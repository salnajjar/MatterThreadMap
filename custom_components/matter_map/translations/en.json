{
  "config": {
    "step": {
      "user": {
        "title": "Matter Thread Map",
        "description": "Add a sidebar panel that maps Matter devices connected over Thread."
      }
    },
    "abort": {
      "already_configured": "Matter Thread Map is already configured."
    }
  }
}
